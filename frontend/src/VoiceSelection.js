const VoiceSelection = ({nextStep}) => {
    return (
        <>
        <button className="btn btn-primary m-2" onClick={nextStep}>
            Submit Voices
        </button>
        </>
    )
}

export default VoiceSelection